[project]
name = "galaxy-tool-xml-check"
version = "0.0.1"
description = "Advisory (detect-only) IUC best-practice checks for Galaxy tool XML — tier 3.5 of the galaxy-tool-xml ecosystem."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Richard Burhans" }]
# Advisory checks are read-only tree queries over tier-1 (`ToolDocument`) that
# report findings as the shared tier-0.5 `Violation`. They do NOT depend on the
# mutating tiers (codemod / fmt) — the app composes this alongside them.
dependencies = [
    "galaxy-tool-refactor-rules",
    "galaxy-tool-xml",
    "lxml>=5",
    "packaging>=23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.5",
    "mypy>=1.10",
]

[tool.ruff]
src = ["src"]
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "PTH"]

[tool.mypy]
files = ["src"]
strict = true

[[tool.mypy.overrides]]
# lxml ships no type information.
module = ["lxml.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
galaxy-tool-refactor-rules = { workspace = true }
galaxy-tool-xml = { workspace = true }
