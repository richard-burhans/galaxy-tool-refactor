"""Maintainer scripts for the galaxy-tool-source workspace (not part of any installed package).

This package marker exists only so the test suite can import the reusable
invariant checks from ``scripts.corpus_check``.
"""
