# Gate re-accumulation statistics

_Generated by `python -m scripts.gate_reaccumulation` on 2026-06-14; swept 459 merged PRs from `galaxyproject/tools-iuc` (PR corpus retrieved 2026-06-14)._

Reproduced by: `python -m scripts.fetch_iuc_prs --state closed --merged-only --corpus-name galaxyproject__tools-iuc__merged --limit 0` then `python -m scripts.gate_reaccumulation`

## What this measures

For every **recently merged** PR, we evaluate the tool in its **merged (`head`) state** — the bytes that actually landed — and ask whether a pre-merge normalization gate (conference §7) would *still* flag it. A flagged merged PR is one whose author left the tool non-canonical even after a full human review cycle: direct evidence the canonical-form backlog re-accumulates without forward enforcement.

Of **459** merged PRs swept, **452** changed at least one file that parses as a `<tool>` (PRs touching only `macros.xml` or non-tool XML are excluded from the denominator; 0 tool files were skipped on a parse/detect error).

## Headline — share of merged PRs the gate would have flagged

| Gate variant | Flagged merged PRs |
|---|---|
| cosmetic (GTR001, GTR004) | 356/452 (78.8%) |
| full (all default fixable rules) | 437/452 (96.7%) |
| full minus attribute order (GTR002) | 372/452 (82.3%) |

**65** of the flagged PRs are flagged *only* by attribute order (`GTR002`) — the population that stays blocked until IUC blesses a canonical order (conference §3). Every other flagged PR would be caught by a gate we could ship today.

## Per-code re-accumulation

How many merged PRs still carry each rule's finding in their merged state:

| Code | Merged PRs | Tool files |
|---|---|---|
| GTR002 | 410 | 658 |
| GTR001 | 348 | 595 |
| GTR020.1 | 66 | 89 |
| GTR004 | 49 | 60 |
| GTR019.1 | 25 | 48 |
| GTR005 | 19 | 54 |
| GTR037 | 3 | 5 |
| GTR013 | 2 | 14 |
| GTR018.1 | 1 | 1 |
