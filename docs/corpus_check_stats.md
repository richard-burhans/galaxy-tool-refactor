# Corpus check (unified detect) statistics

Generated by `python -m scripts.corpus_check check` on 2026-05-29. Runs the exact detect phases the `galaxy-tool-refactor check` command reports — the canonical codemods + cosmetic fmt rules (*fixable*) plus the advisory IUC best-practice checks (*advisory*, detect-only) — across the combined corpus (github + toolshed, sha256-deduplicated), and counts, per rule, how many tools carry the finding. Unlike the per-rule **isolation** page (`corpus_rule_stats.md`, which counts every emitted edit to QA each rule alone), these are genuine per-occurrence violation counts. Every parseable `<tool>` is swept (no validity gate, matching `check`).

**9,289** tools swept; **9,289** have at least one finding (**9,287** a fixable one, **9,037** an advisory one); 0 crashed.

Regenerated by every full run of `corpus_check.py check` unless `--no-stats` is given; partial sweeps (`--limit` or `--repo`) do not regenerate it.

## Fixable rules (GTX — what `format` would change)

Structural canonical codemods and cosmetic fmt rules. A finding here means `format` would change the tool; `check` exits non-zero on any of these.

| Rule | Tier | Tools flagged | % of tools | Findings | What it flags |
|---|---|---:|---:|---:|---|
| GTX001 | fmt | 6,656 | 71.7% | 311,236 | Canonical 4-space indentation; no tabs. |
| GTX002 | codemod | 6,627 | 71.3% | 37,334 | Reorder every `<param>` element's attributes to the IUC convention. |
| GTX003 | fmt | 9,230 | 99.4% | 69,842 | One blank line between top-level children of `<tool>`. |
| GTX004 | fmt | 1,219 | 13.1% | 2,130 | Collapse empty-with-whitespace leaves to `<foo/>` form. |
| GTX005 | codemod | 1,064 | 11.5% | 1,064 | Reorder the root `<tool>` element's attributes to the documented prefix. |
| GTX006 | codemod | 30 | 0.3% | 30 | Repair near-miss spelling typos so a globally-invalid tool validates. |
| GTX013 | codemod | 5,009 | 53.9% | 5,009 | Reorder `<tool>` child elements to the IUC convention. |

## Advisory checks (IUC — best practices)

Detect-only IUC best-practice checks. Advisory: `check` reports them but does not fail unless `--strict`. `IUC011`/`IUC012` are reserved placeholders (no detection yet), so they flag nothing.

| Rule | Tier | Tools flagged | % of tools | Findings | What it flags |
|---|---|---:|---:|---:|---|
| IUC001 | check | 2,669 | 28.7% | 2,669 | Tool should ship at least one functional `<test>`. |
| IUC002 | check | 3,266 | 35.2% | 3,266 | `<command>` body should be wrapped in CDATA. |
| IUC003 | check | 1,913 | 20.6% | 1,913 | Tool id should use lowercase letters, digits, and '_.+-'. |
| IUC004 | check | 61 | 0.7% | 61 | Tool version should be PEP 440 or a @...@ version macro. |
| IUC005 | check | 5,327 | 57.3% | 5,327 | Tool should declare `<requirements>`. |
| IUC006 | check | 3,810 | 41.0% | 3,810 | Tool should declare error handling (detect_errors or `<stdio>`). |
| IUC007 | check | 8,325 | 89.6% | 8,325 | Tool should declare EDAM topics/operations or `<xrefs>`. |
| IUC008 | check | 614 | 6.6% | 614 | Tool should provide non-empty `<help>`. |
| IUC009 | check | 957 | 10.3% | 957 | Tool should provide a non-empty `<description>`. |
| IUC010 | check | 3,680 | 39.6% | 3,680 | `<help>` body should be wrapped in CDATA. |
| IUC011 | check | 0 | 0.0% | 0 | Single-quote Cheetah variables in `<command>` (not yet implemented). |
| IUC012 | check | 0 | 0.0% | 0 | Join shell commands with && not a lone & (not yet implemented). |
