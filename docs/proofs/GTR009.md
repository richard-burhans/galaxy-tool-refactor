# GTR009 — Upgrade24_0 (upgrade step)

**Contract:** structural-only.
## Claim (contract)
Structural-only 24.0-step normalization (see [GTR007](GTR007.md)).

## Proof note (refutation overreach, resolved)
The 2026-06-04 adversarial pass claimed the filter-hoist drops a post-comment
tail; re-verified against Galaxy source: Galaxy itself evaluates
`eval(filter.text.strip())` — only `.text` — so the dropped tail was **dead at
runtime**. Behaviour-preserving; no code change (PR #115).

