# GTR012 — UpgradeToLatest (orchestrator)

**Contract:** structural-only.
## Claim (contract)
The iterative driver of [GTR007](GTR007.md) + the single-step `upgrade_vN`
codemods; structural-only by composition — every constituent step is (codemod
§22). Runtime deltas surface via `behavior_preserving` + §23 warnings.

