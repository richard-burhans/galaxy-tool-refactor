# GTR008 — Upgrade19_01 (upgrade step)

**Contract:** structural-only.
## Claim (contract)
**Structural-only** single-step normalization for the 19.01 → 19.05 schema delta:
XSD-valid at the reached profile + idempotent; runtime surfaced via
`behavior_preserving` (see [GTR007](GTR007.md)).

## Proof
Grown empirically from `corpus_check codemod` discovery sweeps and pinned by
per-step tests + per-rule corpus isolation (idempotence + post-validity, 0
failures). Codemod §22.

