# GTR006 — FixTypos (codemod)

**Contract:** validity restoration.
## Claim (contract)
**Validity restoration**, not runtime preservation of invalid tools: the rule
fires only on a tool that is XSD-invalid at every profile, repairing near-miss
attribute values toward the schema's vocabulary.

## Proof
A tool invalid everywhere has no defined Galaxy behaviour to preserve; the
repair makes it validatable. The 2026-06-04 adversarial refutation
(case-folding `format="RestructuredText"`) **overreached** — re-verified: the
input value was invalid at every profile, so the contract applies (ledger,
GTR006 entry). Vocabulary comes from the generated model for the tool's own
profile with a macro skip-set.

## Scope boundary
Never fires on a tool valid at any profile.

